# 뉴스 중요도 「공통 기준 + 팀별 기준」 재설계안 (2026-09-25, 승인 대기)

- 상태: **2026-09-25 운영자 승인 — D1~D9 전부 권고안대로.** 1단계 구현은 Opus 5.5 새 세션에서(구현 인계 `docs/인계_구현_긴급도규칙_1단계_260925.md`). 2단계(팀 층 ①~⑦)는 1단계가 1~2주 돌고 10월 국감 보도로 규칙을 본 뒤 Fable 세션에서 결정.
- **1단계 구현 완료(2026-09-25, 배경역사 #216)** — 표·컬럼·D8 5개 규칙·크롤러·출처 표시·편집기·소급(값 변경 160, 기록 1,017) 배포. **구현 중 바뀐 것 하나: 편집기 위치는 설정 탭이 아니라 뉴스 화면 머리줄의 「긴급도 설정」 버튼이 여는 창**(운영자 결정 — 설정 탭은 관리자 전용이라 2단계 팀장이 못 들어오고, 목록 위 카드는 뉴스를 가린다). 아래 §4-4의 「설정 탭」은 이 뜻으로 읽을 것.
- 배경역사 번호: **#216 예정** — 파일의 마지막 항목은 #214(Supabase GRANT), 다른 창이 자문 규칙 통일(bd3ce5c)에 #215를 쓸 예정이라 아직 파일에 없다. 커밋 직전에 파일을 다시 읽어 번호를 확정한다.
- 모델 권고: 이 항목은 **Fable 권장**(두 판·여러 파일에 걸친 흐름 변경 + 팀별 AI 판정 구조 결정 + 배경역사 번호 신설). 현재 세션이 Fable이므로 전환 없음. 승인 뒤 1단계 구현은 절차형이라 Opus 5.5로도 충분하지만, 같은 세션에서 이어가는 편이 전환 비용(컨텍스트 재적재)보다 싸다.
- 사내판: **필요 — 사내 콘솔에도 팀별 규칙 편집기를 둔다(2026-09-25 운영자 결정). 공통 규칙은 외부 정본을 받아 읽기만, 사내 팀 규칙은 사내 등급에만, 매처·형식은 공용 파일 이식** — 세부·인계 계획은 §8.
- 관련 기록: 지침 #133·#159(팀별 중요도 예고), #107(기준문 순서·피드백 한계), #161-보론4(사고 축), #44(재알림 억제), #214(새 객체 GRANT), 메모리 「다팀·개인별 중요도 설계 기본안(2026-09-20 결정, B안)」.

---

## 0. 요청 요지

뉴스 중요도를 「공통 기준 + 팀별 기준」으로 재설계한다. 첫 사례: **국정감사에 SK텔레콤(통신3사 일괄 포함)이 증인·참고인으로 채택·소환·출석하는 기사는 모든 팀에 무조건 긴급** — 공통 낱말 규칙.

등급 결정 순서(제안 그대로 채택):

```
사람이 고친 값(팀별) > 팀 낱말 규칙 > 팀 AI 판정 > 공통 낱말 규칙 > 공통 AI 판정(지금의 Haiku)
```

---

## 1. 실측 (2026-09-25 실DB, AI 0회)

### 1-1. 현재 구조에서 등급이 정해지는 자리

| 단계 | 위치 | 내용 |
|---|---|---|
| 공통 AI 판정 | `crawler.py:338-385` `classify_urgency()` | Haiku 4.5, 본문 600자 → 없으면 네이버 요약 → 없으면 제목. 출력 즉시대응/금주검토/동향파악 → `_AI_PRIORITY_MAP`으로 긴급/보통/참고. API 실패 시 키워드 폴백은 **긴급을 내지 못한다**(보통/참고만). |
| 저장 | `crawler.py:1615-1625` | `item['urgency'] = item['importance'] = val` → `news_feed` 벌크 upsert(`on_conflict=url`). **규칙 층은 이 대입 직전에 끼운다.** |
| 운영자 알림·구독자 큐 | `crawler.py:2092-2118` | `urgent_items = [… if i['urgency']=='긴급' …]` — **메모리 값**을 쓴다(DB 재조회 없음). `suppress_repeat_alerts()`(#44) 뒤 텔레그램·메일·`subscriber_queue(topic='urgent')`. |
| 아침 브리핑 | `morning_briefing.py:389-416` | 저장값으로 정렬(긴급>보통>참고), 이월 후보도 `urgency='긴급'`. |
| 구독자 발송 Edge | `send-subscriber-briefing` | 등급을 다시 읽지 않는다 — 큐 행의 html을 그대로 발송. 봇 '더 보기'만 `urgency='보통'` 조회. |
| 대시보드 | `app.js:3673` `IMPORTANCE_RULES`, `:4054-4083` 배지, `:4085` 필터, `:4289` `setNewsImportance` | 단일 값 표시. 수정은 관리자만(`news_feed_edit_guard` 트리거 + `imp_fb_*` 정책, #159). **등급의 출처(AI/규칙/사람)를 보여주는 자리는 없다.** |
| 사람 수정 기록 | `importance_feedback` | 53건(06-12~09-13), 그중 등급 변경 40건. **팀 컬럼 없음.** 09-13 이후 0건. |
| 다른 수집기 | `gov_notice_crawler.py`·`kmcc_meeting.py`·`foreign_press.py` | 등급을 상수로 박아 저장(보통/참고). AI 판정 없음. |

기존에 **낱말로 등급을 강제하는 코드는 없다**(`is_ministry_personnel_news`는 수집·선별 통과용).

### 1-2. 계기가 된 사례의 실제 상태

| 제목 | DB 값 | 사람 수정 기록 |
|---|---|---|
| [2026 국감 증인-과방위①] SKT·KT 보안책임자 증인석… | **긴급** | 없음 (AI 판정) |
| 이통3사·듀오·티빙…'유출'기업 대거 국감소환 | **긴급** | 없음 (AI 판정) |
| 통신 3사, CISO급 나란히 국감행···해킹 1년 '보안 쇄신' 검증 | **보통** | 없음 |
| 과방위 국정감사 '개인정보·플랫폼·AI' 정조준…SKT·구글·스캐터랩… | **보통** | 없음 |

요청문에 인용된 두 건은 현재 DB에서 긴급이고 수정 기록이 없다(운영자가 보신 시점의 값이 달랐을 수 있으나 기록으로는 확인되지 않음). 실제로 낮게 남아 있는 것은 아래 두 건이며, 셋째는 규칙이 잡고 넷째는 제목·요약에 증인·소환류 낱말이 없어 **규칙이 못 잡는다**(AI 층의 몫).

### 1-3. 첫 규칙을 2026-09-15~09-25 기사에 돌린 결과

기간 기사 2,632건(긴급 248 / 보통 1,320 / 참고 1,064). 대상 낱말(SK텔레콤·SKT·통신3사류) 포함 412건, 「국감|국정감사」 포함 49건.

| 규칙 형식 | 적중 | 그중 이미 긴급 | 값이 바뀌는 건 | 오탐 |
|---|---|---|---|---|
| ㉠ 요청안: 대상 any + 소환 and_any(평면 14개) | 10 | 9 | 1 | 0 |
| ㉡ 권고안: 대상 any + [국감·국정감사] + [증인·참고인·소환·출석·국감행·국감장] (그룹 2개 모두 하나 이상) | 11 | 10 | 1 | 0 |

㉡이 추가로 잡은 것: 「과방위 '보안 국감' 칼 빼드나…통신 3사 CISO·티빙·CJ ENM CEO 소환」(㉠은 '소환' 앞에 '국감'이 붙지 않아 놓침). 08-25~09-25로 넓혀도 ㉡ 11건·오탐 0. 값이 실제로 바뀌는 것은 「통신 3사, CISO급 나란히 국감행…」 1건.

→ **AI는 이 사건을 11건 중 10건 맞췄다.** 규칙의 값어치는 "매번 맞추던 것을 확정"하는 데 있고(10월 실제 출석 보도 때 뒤집힘 방지), 비용은 0이다.

### 1-4. 비용 기준선 (api_usage, 최근 7일)

| 콜 | 7일 콜 수 | 일 평균 | 입력/캐시읽기(평균 토큰) | 7일 추정 |
|---|---|---|---|---|
| `classify_urgency`(개별 긴급도) | 1,454 | 208 | 1,098 / 4,514 | ≈ $2.4 |
| `_screen_batch_haiku`(선별) | 744 | 106 | 4,328 / 4,319 | ≈ $4.1 |

팀 하나를 B안(한 콜 다중 출력)으로 붙이면 월 ≈ $2(2026-09-20 결정·09-24 재계산). 규칙 층은 $0.

### 1-5. 팀·계정·구독자 현황

- `teams`: 경쟁제도팀(0명) · 기술정책팀(승인 1명) · AI정책팀(0명). `profiles.team_id`가 이미 있다.
- `telegram_subscribers`: 팀 컬럼 없음. `subscriber_queue.topic` CHECK = urgent/assembly/kmcc.
- `app_config` 쓰기 정책: `is_admin()`만 본다(키 목록 제한 없음 — 지침 표 165·170행의 "press_keywords 키만"은 옛 서술, 실DB와 다름).

### 1-6. 사내판 회신(2026-09-25)으로 드러난 사실

- **사내판은 같은 5층 구조를 2026-09-22부터 이미 운영 중**이다(JSONL 파일, 콘솔·알림 앱 두 함수가 같은 순서, 전체 기사 대조 불일치 0 유지). 이 설계안의 원칙 1~9는 그 운영 실측에서 나온 것이다. 외부 2단계는 사내 의미론을 그대로 따른다 — 특히 「공통 min 하한은 팀 AI가 못 뚫음」(사내에서 팀 AI가 하한 걸린 주파수 기사를 참고로 내린 사고 뒤 9/25 추가).
- 사내에는 DB·팀 표가 없다. 팀 키는 사내 로그인의 **조직 코드 문자열**이고 팀장은 운영자가 명부 파일로 지정한다 → 공유하는 것은 **규칙 본문 형식과 매처**만, 주인 키(`team_id`)는 매처가 보지 않는 불투명 값(외부 smallint, 사내 문자열).
- 사내 다리 `export_news.py`는 명시 컬럼 목록(`COLS`)이다. `urgency_rule`을 받고 싶어 하며, 외부 DB에 컬럼이 생기기 전에 넣으면 PostgREST 400으로 델타가 멈추므로 사내가 먼저 「400이면 새 컬럼 빼고 재시도」 내성을 넣는다(외부 배포 시점과 독립).
- **외부가 판정한 기사는 `urgency_rule`이 정본**이다 — 외부 규칙은 네이버 요약(DB 미저장)을 보고 사내에 오는 `summary`는 다른 텍스트라, 사내가 같은 매처를 다시 돌리면 결과가 갈린다. 사내 재판정은 그 컬럼이 없는 과거 행에만.
- 사내 공통 규칙 5개(부고→보통, SKT 국감 증인→긴급, 부처 인사→보통, 국회·법률→보통, 부처+통신→보통, 전부 min)를 **외부 공통 규칙으로 이관**하고 사내는 읽기 전용으로 받는 방향(운영자 확정 전, 결정 D8). 외부 영향 재측정(08-25~09-24, 제목+`summary`, NFC, 첫 적중 규칙 기준):

| 순서 | 규칙 | 등급 | 적중 | 참고→보통 변경 | 비고 |
|---|---|---|---|---|---|
| 1 | 부고 | min 보통 | 4 | 2 | 「[부고] 전 방통위 국장 모친상」 2건. 맨 앞에 두는 이유는 기관명 든 부고가 3·5번으로 올라가는 것을 막기 위함 |
| 2 | SKT 국감 증인 | min 긴급 | 11 | (보통→긴급 1) | 1-3과 동일 |
| 3 | 부처 인사 | min 보통 | 22 | 16 | |
| 4 | 국회·법률(과방위·국정감사·전기통신사업법) | min 보통 | 96 | 30 | 표본 15건 검토: 전부 보통이 맞는 기사(단통법 폐지·전통법 시행규칙 의견모집·과방위 기사) |
| 5 | 부처+통신 낱말 | min 보통 | 603 | 54 | 기간 기사의 23%가 걸리지만 대부분 이미 보통 이상. 참고였던 표본의 「SKT 신규가입 중단 행정지도」·「SKT 침해사고 미신고 과태료」는 AI 오판이 아니라 **9/24 10:40~10:59 이슈맵 과거기사 반입분**(≈144행, 발행 2021~2026, 본문 0, `urgency_screen` null, 기본값 참고)이다 — 반입 행은 AI 판정을 거치지 않으므로 규칙 재적용(D5)이 이들을 보통으로 올려도 발행일이 옛날이라 알림은 없다 |
| 합 | | | | **102** (사내 측정 107과 근사) | 긴급을 만드는 규칙은 2번뿐 → 텔레그램·구독자 알림은 2번 외 변화 없음 |

---

## 2. 층(layer) 정의와 결정 규칙

등급 순서: 참고(0) < 보통(1) < 긴급(2).

```
                 ┌ 사람 수정(팀)  ─ 그대로 (공통 규칙 하한도 뚫을 수 있음)
 팀 T의 값  =    ├ 팀 규칙 set    ─ 그 값 (팀 AI 생략)
                 ├ 팀 규칙 min    ─ 하한만 건다 → 팀 AI는 돈다
                 ├ 팀 AI          ─ max(팀 AI, 팀 min 하한, 공통 min 하한)   ← 공통 set 적중이면 생략
                 └ (팀 지침 없음) ─ 공통값
 공통값      =   ┌ 관리자 수정(공통) ─ 그대로
                 ├ 공통 규칙 set  ─ 그 값 (공통 AI 생략)                      ← 적중 id 기록
                 ├ 공통 규칙 min  ─ 하한만 → 공통 AI는 돈다, 결과 = max(AI, 하한) ← 적중 id 기록(값 불변이라도)
                 └ 공통 AI        ─ classify_urgency
```

**적중과 AI 재판정의 관계(2026-09-25 사내 회신 2로 정리, 운영자 결정 D9)** — 요청문 원칙 6("걸린 기사는 판정 대상에서 뺀다")을 다음과 같이 좁힌다:
- **min 적중 → 그 층 AI를 돌린다.** 결과 = max(AI 값, 하한). 뒤집기는 하한이 막는다(원래 긴급인 주파수 기사를 AI가 참고로 내린 사내 사고는 하한만으로 막힌다). `urgency_rule`은 적중 기록으로 그대로 남긴다.
- **set 적중 → 그 층과 그 아래 층 AI를 건너뛴다**(set은 운영자가 등급을 못 박은 것). 공통 set에 동의하지 않는 팀은 팀 규칙(set)이나 사내 「다르게/끄기」로 뺀다 → 그 팀 AI가 다시 돈다.
- **하한은 위 층에도 걸린다**: 공통 min 하한 아래로 팀 AI도 못 내린다. 팀 규칙 set과 사람 수정만 내릴 수 있다.
- 이유: 5번 규칙(부처+통신)처럼 넓게 걸리는 min 규칙까지 AI를 빼면 팀 AI가 도는 범위가 23% 줄어든다. 사내 팀 AI는 이미 이 방식(공통 적중 기사도 판정, 9/25부터 하한 적용)이고, 사내 공통 AI만 「공통 set 적중만 생략」으로 맞출 예정.

- **공통 규칙은 팀 설정이 없어도 전원에게 적용된다(2026-09-25 운영자 지시).** 1단계는 팀 층이 없으므로 공통 규칙 결과가 곧 모든 이용자·알림·브리핑이 보는 값이다. 2단계 뒤에도 팀 지침·팀 규칙·팀 값이 없는 이용자(비로그인·팀 미지정·지침 없는 팀)는 공통 규칙이 적용된 공통값을 그대로 본다. 팀 설정은 공통값 위에 덧씌우는 것이고, 공통 규칙을 끄는 방법은 그 팀의 set 규칙 또는 사람 수정뿐이다.
- **min은 올리기만, set은 지정.** 올리려던 규칙이 AI의 더 높은 등급을 끌어내리지 않는다.
- **처음 걸린 규칙 하나가 정한다.** 배열 순서 = 우선순위. 강한 규칙을 앞에.
- **「걸렸다」와 「값이 바뀌었다」를 구분한다.** 걸린 기사는 `urgency_rule`(규칙 id)에 값이 안 바뀌어도 남긴다(출처 표시·통계·사내 정본). AI 생략은 set 적중에만(위 D9).
- **규칙은 제목 + 네이버 요약만 본다.** 본문은 보지 않는다(과거 사례 언급 오탐).
- **화면과 알림은 같은 저장값**을 쓴다(1단계). 2단계 팀 덧씌우기도 함수 하나(`effectiveUrgency`)로 통일하고 배포 전 전체 기사로 대조한다.
- **어느 층에서 온 값인지 화면에 표시**한다: `AI 판정` / `규칙: <note>` / `담당자 수정` (+ 2단계: `팀 규칙`·`팀 AI`·`팀 담당자`).

---

## 3. 규칙 형식 (데이터, 표 `urgency_rules` — 2026-09-25 운영자 회신으로 app_config 키에서 표로 변경)

저장은 **표 한 개**, 행 하나가 규칙 하나다. 팀 칸(`team_id`, null = 공통)을 1단계부터 둔다 — 팀장이 자기 팀 규칙만 고치는 DB 정책은 키 한 줄(app_config)로는 못 건다.

```sql
create table public.urgency_rules (
  id          text primary key,                  -- 'skt_gukgam_witness'
  team_id     smallint references public.teams(id),   -- null = 공통
  position    int  not null default 100,         -- 작을수록 먼저 (배열 순서 대신)
  mode        text not null check (mode in ('min','set')),
  level       text not null check (level in ('긴급','보통','참고')),
  any_words   jsonb not null,                    -- ["SK텔레콤", …]
  and_any     jsonb not null default '[]',       -- [["국감","국정감사"],["증인",…]]  (평면 배열도 허용)
  none_words  jsonb not null default '[]',
  note        text not null default '',
  enabled     boolean not null default true,
  updated_by  uuid, updated_at timestamptz not null default now()
);
-- RLS: SELECT anon·authenticated — 공통·팀·enabled=false 모두(조건 없음). 낱말 목록이라 비밀 아님. 사내 다리가 anon 키로
--      공통 행을 enabled 무관하게 전부 받아 「원본 삭제됨」 표시에 쓴다(사내 회신 2) — anon SELECT에 enabled=true 조건을 넣지 말 것
--      INSERT/UPDATE/DELETE authenticated: 공통(team_id is null) → is_admin() / 팀 → is_admin() or (is_leader() and team_id = my_team())
--      service_role: 전권(크롤러 읽기). GRANT 3역할 명시(#214). 시퀀스 없음.
```

규칙 한 줄을 JSON으로 보면(편집기·크롤러가 다루는 모양):

```json
[
  {
    "id": "skt_gukgam_witness",
    "min": "긴급",
    "any": ["SK텔레콤", "SKT", "통신3사", "이통3사", "통신 3사", "이통 3사", "이동통신 3사"],
    "and_any": [
      ["국감", "국정감사"],
      ["증인", "참고인", "소환", "출석", "국감행", "국감장"]
    ],
    "none": [],
    "note": "국정감사 증인·참고인 채택·소환·출석 — 전 팀 공통 긴급 (2026-09-25 운영자 규칙)"
  }
]
```

- 판정: `any` 중 하나 이상 **AND** `and_any`의 **각 그룹**마다 하나 이상 **AND** `none` 없음. `and_any`는 평면 배열도 허용(그룹 하나로 해석) — 요청안 형식과 호환.
- `min` 또는 `set` 중 하나. `id`는 표시·집합 관리용(필수). `note`는 화면에 그대로 보인다.
- 매칭은 부분 문자열(한국어 단어 경계 없음). do-not #99(`AI`→`KAIST` 오탐)와 같은 함정이 있으니 **규칙을 넣기 전에 최근 30일 기사에 돌려 오탐을 본다**(1-3처럼). 2~3자 낱말(`증인`·`소환`)은 반드시 다른 그룹과 AND로만 쓴다.
- 코드에 같은 규칙을 **FALLBACK 상수**로도 둔다 — 표 조회 실패 시 비상용(#53 「DB가 원본, 코드는 폴백」과 같은 구조).

**매처 계약(사내판 회신 반영, 두 판 공통)**
- 입력 텍스트 = `NFC 정규화(제목 + " " + 요약)`. 사내 파일은 맥·윈도를 오가 NFD가 섞인다 — 외부 매처도 같은 정규화를 넣는다.
- 반환 = `(등급, 걸린 규칙 id 또는 None, 값 변경 여부 bool)` 셋. 사내 세 지점(적재 색인·화면 계산·알림 계산)이 모두 쓴다.
- **첫 적중 규칙이 정한다** = 첫 적중이 min이고 이미 그 이상이면 **값 그대로, 뒤 규칙으로 넘어가지 않음**(사내 현행). 케이스 파일에 못 박는다.
- 적중 기록(`urgency_rule`)은 **값이 바뀌지 않아도** 남긴다 — 사내가 「공통 규칙이 답한 기사」 표시로 써서 팀 AI가 뒤집지 못하게 막는다(사내 실제 사고: 원래 긴급인 주파수 기사를 min 규칙이 그대로 두자 팀 AI가 참고로 내림).
- 규칙 `id`는 **영구 불변**(이름을 바꿔도 id 유지, 지운 id 재사용 금지) — 사내 팀 사본이 id로 원본을 가리킨다. 편집기는 id를 만들 때 자동 생성(`slug_YYMMDD`)하고 수정 화면에서 id 칸은 잠근다.
- 삭제는 **`enabled=false`만, 행 삭제 없음**(사내가 공통 행을 통째로 받아 비교하므로 한 가지로 고정). 행마다 `updated_at`(있음) — 사내 「다르게」 사본이 원본 변경을 알아채는 기준.
- `team_id`는 매처가 보지 않는다. 케이스 파일(`tests/fixtures/urgency_rules_cases.json`)에도 넣지 않는다.
- 끄기·팀 사본 치환은 **목록을 만드는 쪽**(외부: 로더 / 사내: 상속 편집기)이 하고 매처는 받은 목록을 순서대로 돌기만 한다 — `position`은 치환 뒤에도 유지(첫 적중이 정하므로 자리가 바뀌면 안 된다).

---

## 4. 1단계 — 공통 규칙 층 (먼저, 작게)

### 4-1. 크롤러 (`crawler.py`)

1. `load_urgency_rules()` — `load_news_criteria()`(1128행)와 같은 try/except·폴백 패턴: 표 `urgency_rules`에서 `enabled` 행을 `position` 순으로 읽고(1단계는 `team_id is null`만) 형식 검증(id·mode/level·any 필수, 등급값 셋 중 하나) → 실패·빈 값이면 `URGENCY_RULES_FALLBACK`. 실행당 1회. 로드 결과를 `[규칙] N개 로드(db|fallback)`로 로그.
2. `apply_urgency_rules(rules, title, summary, ai_val) -> (val, rule_id)` — **순수 함수**(sb·네트워크 없음, 테스트 가능).
3. `save_new_items` 1615행 루프 — **규칙을 AI보다 먼저** 돌려 set 적중이면 콜을 생략한다(D9):
   ```python
   hit = match_urgency_rules(_RULES, item['title'], _URGENCY_SUMMARY.get(url, ''))   # (rule_id, mode, level) or None
   if hit and hit.mode == 'set':
       val = hit.level                                   # 공통 AI 생략
   else:
       val = classify_urgency(...)
       if hit: val = max_level(val, hit.level)           # min 하한
   item['urgency'] = item['importance'] = val
   item['urgency_rule'] = hit.rule_id if hit else None   # 모든 행에 키 존재(벌크 upsert 제약, #82); 값 불변이라도 기록
   ```
   AI 폴백 경로(API 장애)에도 규칙은 그대로 적용된다 — 규칙 층은 API 없이 돈다. 첫 규칙은 min이므로 1단계에서 콜 수는 줄지 않는다.
4. 로그 `[규칙] 적중 N건 (skt_gukgam_witness ×N, 값 변경 M건)` — 조용한 실패 금지 원칙.
5. 알림·큐·브리핑은 저장값을 쓰므로 **코드 변경 없이 따라온다.** 재알림 억제(#44)도 그대로 거친다(같은 소환 보도 9건이 1건 + 리마인드로 나가는 현행 동작이 맞다).
6. `_URGENCY_CRITERIA` 즉시대응 목록에 같은 뜻의 항목 추가(낱말이 안 걸리는 변형 대비). 위치는 **「침해사고·장애·유출」 항목 바로 뒤, 「제도가 실제로 움직인 것」 앞** — 첫 항목은 사고 축이어야 하고(#161-보론4), 우선 규칙은 앞쪽에(#107):
   > - **국정감사에 SK텔레콤이(또는 통신3사가 일괄로) 증인·참고인으로 채택·신청·소환·출석하는 보도** — 채택 단계든 출석 당일이든 즉시대응. 타사만 소환된 국감 기사는 이 항목이 아니다(다른 항목으로 판단).
   기준문이 바뀌면 캐시 접두가 1회 재작성된다(1시간 캐시, 비용 미미). do-not(#161-보론2)에 따라 **`importance_feedback` 53건을 세션에서 재채점해 일치율 확인**(API 0회).

### 4-2. DB

- `alter table news_feed add column urgency_rule text;` — null = 규칙 미적중. 크롤러(service_role)만 쓴다. `authenticated`의 컬럼별 UPDATE 권한(8칸, #191)에 넣지 않는다 → 화면에서 못 고친다(의도). 기존 테이블의 새 컬럼은 #214 무관이지만 커밋 전 `tools_release.py` ⑥으로 확인.
- `urgency_rules` 표 생성(§3 DDL) + RLS + **GRANT 3역할 명시**(#214, `tools_release.py` ⑥으로 확인) + 첫 규칙 1행 삽입(세션 `sb_client` 스크립트 또는 편집기). 크롤러는 `enabled` 행을 `position` 순으로 읽는다. 1단계 크롤러는 **공통 규칙(team_id null)만** 적용한다 — 팀 규칙은 팀 값 저장소(2단계 ②)가 생겨야 적용할 자리가 있다.
- 지침 DB 표에 두 항목 추가(`news_feed.urgency_rule`, `urgency_rules`).

### 4-3. 과거 기사 재적용 (운영자 결정 D5)

- 대상: 최근 60일 기사 중 규칙 적중이고 `urgency`가 규칙보다 낮고 `importance_feedback`에 없는 것. **현재 1건**(「통신 3사, CISO급 나란히 국감행…」). 적중이지만 이미 긴급인 10건도 `urgency_rule`만 채운다(출처 표시·집합 관리용).
- 방법: 세션이 `sb_client` 스크립트로 UPDATE(MCP는 읽기 전용). **기존 행 UPDATE는 알림을 만들지 않는다**(#161-보론5 ①②) — 크롤러는 그 실행의 신규분만 알리고, 구독자 큐는 크롤러만 적재한다. 당일 브리핑 🔴 동기화는 하지 않는다(과거 날짜).

### 4-4. 대시보드 (`app.js`·`index.html`, 선택 — 결정 D3·D4)

- **출처 표시(D3, 권고 포함)**: `NEWS_LIST_COLS`에 `urgency_rule` 추가. 상세 모달 배지 옆(4396행 부근)에 작은 글자로 `AI 판정` / `규칙: <note>` / `담당자 수정 <날짜>`. 담당자 수정 여부는 `importance_feedback`(anon 열람 가능, 53행)을 목록 로드 때 1회 읽어 `news_id` 집합으로 판단. 규칙 note는 `urgency_rules`를 함께 읽어 id→note 매핑.
- **규칙 편집기(D4, 권고 포함·작게)**: 설정 탭의 「수집 키워드 관리」 카드(`app.js:7379-7479`)와 같은 구조로 「긴급도 규칙」 카드 — 위에 **「공통 / 내 팀」 선택**, 규칙 목록(행 단위 추가·수정·순서·켬끔) + 형식 검증 + **「최근 기사에 적용해 보기」**(같은 매처를 JS로 두고 `newsDataCache` 제목+요약에 돌려 적중 목록·값 변경 건수를 저장 전에 보여준다) + 저장. 화면 게이트는 안내용이고 관문은 RLS다(관리자 = 공통+모든 팀, 팀장 = 자기 팀, 팀원 = 열람). **1단계에서는 「내 팀」 탭을 회색으로 두고 "팀별 등급 저장이 열리면 사용"이라 적는다** — 저장은 되지만 적용될 자리가 없어 "고쳤는데 변화가 없다"는 오해를 막기 위함. 2단계 ②·④와 같은 회차에 켠다. 매처 JS는 Python `apply_urgency_rules`와 **같은 규칙**이어야 하며 단위 테스트의 6개 제목을 양쪽에서 돈다.
- **매처를 공용 파일 하나로**: `supabase/functions/_shared/urgency_rules.js`(plain JS — `cite_verify.js`처럼 브라우저·Deno가 같이 로드) + Python `urgency_rules.py`(크롤러·사내판 이식용). 두 구현은 `tests/fixtures/urgency_rules_cases.json`(제목·요약·AI등급 → 기대 등급·규칙 id·변경 여부) 한 파일을 같이 돈다 — 사내판도 이 파일로 검증한다. 케이스에 반드시 넣을 것: ① 걸려야 하는 제목 3·걸리면 안 되는 제목 3 ② min이 긴급을 내리지 않음 ③ set이 내림 ④ none 제외 ⑤ **첫 적중 min이 이미 그 이상이면 값 그대로·뒤 규칙 미적용**(사내 요청) ⑥ **NFD 입력 1건**(사내 요청) ⑦ 평면 and_any 호환 ⑧ **min 적중 + AI가 더 높음 → AI 값** ⑨ **min 적중 + AI가 더 낮음 → 하한** ⑩ **set 적중 → set 값(AI 무시)**(사내 회신 2).
- 캐시 버스터 갱신(`app.js?v=`, 4-4를 하면 `index.html`도).

### 4-5. 테스트·검증

- `tests/test_smoke.py`에 `TestUrgencyRules`: 걸려야 하는 제목 3(「[2026 국감 증인-과방위①] SKT·KT 보안책임자 증인석…」, 「이통3사·듀오·티빙…'유출'기업 대거 국감소환」, 「통신 3사, CISO급 나란히 국감행…」) / 걸리면 안 되는 제목 3(「KT, 국감서 보안체계 점검…이상운 CISO 증인 채택」(SKT 없음), 「SK텔레콤 독과점…해킹에 흔들린 아성 [연도별 국감]」(소환류 없음), 「[2026 국감] 통신3사, '보안 투자' '소비자 혜택 축소' 쟁점」) + min이 긴급을 내리지 않음 + set이 내림 + none 제외 + 첫 규칙 우선 + 평면 and_any 호환.
- 배포 전: 규칙 로더를 실DB에 돌려 09-15~09-25 적중 11·오탐 0 재확인(1-3과 동일해야 함).
- 배포 후 첫 실행 로그에서 `[규칙] N개 로드(db)` 확인. `python -m unittest discover -s tests`, `py -3.12 tools_release.py`.

### 4-5-1. 적용 범위 확인 (운영자 지시)

1단계 배포 즉시 공통 규칙(D8 ⓐ면 5개)은 **팀 설정과 무관하게 전 기사·전 이용자에게 적용**된다 — 크롤러가 `news_feed.urgency`에 규칙 결과를 저장하므로 대시보드(로그인 여부 무관)·운영자 텔레그램·구독자 큐·아침 브리핑·사내 다리가 모두 같은 값을 받는다. 「내 팀」 탭이 회색인 것은 팀 규칙 저장을 미루는 것뿐이고 공통 규칙 효력과는 무관하다.

### 4-6. 1단계에서 바뀌지 않는 것

- `news_feed_edit_guard`·`imp_fb_*` 정책(관리자 전용) — 2단계에서 다룬다.
- Edge Function 배포 없음(등급을 재판정하는 함수가 없다).
- 정부 공고·방미통위·해외 수집기의 상수 등급 — 범위 밖(SKT 국감 소환 기사가 그 경로로 들어올 일이 없음).

---

## 5. 2단계 — 팀 층 (설계만, 운영자와 결정)

전제: 2026-09-20 결정 **B안**(한 콜에 공통 + 팀별 등급을 함께 출력, 기준문은 하나, 팀은 관점 몇 줄 + 팀 피드백 사례로만 다르게) 위에 규칙·수정 층을 얹는다. `news_feed`의 공통 값은 건드리지 않고 팀 값은 **읽을 때 덧씌운다.**

| 결정 항목 | 안 | 권고 |
|---|---|---|
| ① 이용자→팀 | `profiles.team_id`(이미 있음) 그대로. 팀 없는 승인 계정·비로그인은 공통값 | 채택. 새 테이블 불필요 |
| ② 팀 값 저장 | `team_urgency(news_id, team_id, urgency, source in ('human','rule','ai'), rule_id, set_by, updated_at)` PK(news_id, team_id). RLS: SELECT는 로그인(`authenticated`)·자기 팀 또는 관리자, INSERT/UPDATE는 `is_approved_user() and team_id = my_team() and source='human'`, service_role 전권. anon 정책 없음. **GRANT 명시(#214)**. 60일 정리 시 `news_feed` 삭제와 함께 cascade | 채택. `news_feed_edit_guard`는 유지(공통값은 계속 관리자만) — #159가 예고한 "트리거 drop"은 하지 않고, 승인 계정의 수정 통로를 팀 표로 돌린다 |
| ③ `importance_feedback` 팀 분리 | `team_id smallint null` 추가. **null = 공통(관리자가 공통값을 고친 것)**, 기존 53행은 null 유지. 공통 AI 블록은 null 행만, 팀 블록은 자기 팀 행만 읽는다(등급별 최신 12건, 증류 없이 사례 그대로 — `_feedback_fixed_block` 방식) | 채택 |
| ④ 대시보드 덧씌우기 | 승인 로그인 + 팀이 있으면 `team_urgency`를 자기 팀만 조회(order+range 페이징)해 `_importance`에 덧씌움. 배지 옆 출처에 `팀 담당자/팀 규칙/팀 AI`. 상세 모달 셀렉터: 승인 계정 → 팀 값 저장, 관리자 → 「공통 / 내 팀」 선택. 필터 탭은 덧씌운 값 기준. `syncBriefingUrgency`는 공통값에만 | 채택 |
| ⑤ 텔레그램 구독자에 팀 | (a) 2단계에서는 구독자 알림은 공통값 그대로 (b) `telegram_subscribers.team_id` + 큐 행에 팀별 등급을 실어 발송 측이 걸러냄 | **(a)**. 구독자 6명이 한 팀이고, (b)는 큐 형식·발송 Edge·5곳 갱신 규칙(#154)이 함께 움직인다. 팀이 실제로 늘어난 뒤 재검토 |
| ⑥ 팀 AI 비용 상한 | B안이면 팀당 월 ≈ $2(출력 몇 토큰 + 팀 블록 캐시 읽기). 별도 상한 대신 기존 워치독 #211(24h 비용 > 7일 중앙값×2)로 감시 + 팀 지침이 있는 팀만 출력 + 팀 수 상한 3 | 채택. 팀 지침(관점 몇 줄)이 없는 팀은 출력 자체를 안 받는다 |
| ⑦ 팀 규칙 | 표 `urgency_rules.team_id`(§3, 1단계부터 있음). 편집은 관리자(공통+모든 팀) + 팀장(자기 팀). 크롤러는 팀 규칙을 팀 값(②)에 적용하고 적중 id를 `team_urgency.rule_id`에 남긴다 | 편집기 「내 팀」 탭을 ②·④와 같은 회차에 켠다(2026-09-25 운영자: 팀별 편집기 두기로) |

팀 AI 판정 대상에서 빼는 것은 **그 팀 사람 수정 + set 적중(공통·팀)** 이다(D9). min 적중 기사는 팀 AI가 판정하되 결과는 max(팀 AI, 공통 min 하한, 팀 min 하한). B안은 콜이 하나이므로 "생략"은 그 팀 줄의 출력을 저장하지 않는 것이다.

검증(원칙 8): `effective_urgency(row, team_rows, rules)`를 Python(`tools_urgency_audit.py`)과 JS(app.js)에 같은 규칙으로 두고, 배포 전 최근 30일 전체 기사 × 팀으로 두 구현의 결과를 대조해 **불일치 0**을 확인한다. 텔레그램 경로는 ⑤(a)에서 공통값이므로 대조 대상은 대시보드 덧씌우기 vs Python 기준 구현이다.

---

## 6. 운영자 결정 목록 (1단계)

| # | 물음 | 권고 |
|---|---|---|
| D1 | 규칙 형식: ㉠ 요청안(평면 and_any) / ㉡ 그룹 중첩 허용(3그룹) | **㉡** — 11/11 적중·오탐 0, 평면도 그대로 받아 호환 |
| D2 | 저장소: `app_config` 키 / 새 표 | **표 `urgency_rules`**(2026-09-25 회신 반영) — 팀별 편집기를 두려면 행마다 팀 칸과 RLS가 필요. GRANT 명시(#214) |
| D3 | `news_feed.urgency_rule` 컬럼 추가(출처 표시·적중 집합) | **추가** — 없으면 원칙 3(걸린 집합)·9(출처 표시)를 못 지킨다 |
| D4 | 편집기 + 미리보기를 1단계에 포함 | **포함** — 편집기 하나에 「공통 / 내 팀」 선택. 1단계는 공통만 켜고 「내 팀」은 2단계 ②·④와 함께(팀 값 저장소가 없으면 적용될 자리가 없다). 사내판도 같은 형식의 팀별 편집기(§8) |
| D5 | 과거 기사 재적용 | **적용** — 값 변경 1건 + 출처 표기 10건, 알림 0 |
| D6 | 기준문 추가 위치 | **「침해사고」 항목 뒤, 「제도」 항목 앞** |
| D7 | 규칙 적중 기사도 재알림 억제(#44) 그대로 | **그대로** |
| D9 | 규칙 적중과 AI 재판정: ⓐ 요청문 원칙 6 그대로(min·set 모두 AI 생략) ⓑ **min은 AI 돌리고 하한만, set만 AI 생략**(§2) | **ⓑ** — 뒤집기는 하한이 막고, 넓은 min 규칙(5번, 23%)이 팀 AI 범위를 깎지 않는다. 사내 팀 AI 현행과 같아 두 판이 한 원칙이 된다 |
| D8 | 사내 공통 규칙 5개(§1-6)를 외부 공통 규칙으로 받아들일지 — ⓐ 5개 전부 ⓑ 2번(SKT 국감)만 먼저, 나머지는 사내에 남김 ⓒ 2번 + 3·4·5번, 부고는 제외 | **ⓐ 권고** — 사내가 읽기 전용으로 받으려면 외부에 있어야 하고, 긴급을 만드는 규칙은 2번뿐이라 알림은 안 바뀐다. 참고→보통 하루 3~4건. 부고(1번)를 빼면 사내는 그 줄을 팀 규칙으로 남겨야 한다 |

2단계 ①~⑦은 1단계가 1~2주 돌고 10월 국감 실제 보도로 규칙이 맞는지 본 뒤 결정을 권고한다.

---

## 7. 문서 갱신 (구현 시 전문 제공)

- **CLAUDE.md**: `crawler.py` 항목에 「⑤ 긴급도 = Haiku 판정 → 표 `urgency_rules` 낱말 규칙(min/set, 제목+요약) → 저장; 적중은 `news_feed.urgency_rule`」 한 줄.
- **지침(운영핵심)**: DB 표에 `news_feed.urgency_rule`·표 `urgency_rules`(팀 칸·RLS) 추가; `app_config` 정책 서술(165·170행 옛 문장) 실DB와 일치시키기; do-not 신규 항목(맨 위):
  > - **긴급도 낱말 규칙(표 `urgency_rules`, #216, 2026-09-25)을 코드 상수로 되돌리거나, `min` 규칙이 AI의 더 높은 등급을 내리게 바꾸지 말 것 / 규칙은 제목+요약만 본다(본문 금지) / 적중은 값이 안 바뀌어도 `news_feed.urgency_rule`에 남긴다 / 새 규칙은 최근 30일 기사에 먼저 돌려 오탐 0을 확인한다** — 규칙 순서가 우선순위라 강한 규칙을 앞에 둔다(뒤에 두면 약한 규칙에서 멈춘다). 본문까지 보면 과거 사례로 언급된 회사가 걸린다. 적중 집합은 2단계 팀 AI가 뒤집지 않게 하는 근거다. 매칭은 부분 문자열이라 2~3자 낱말은 AND 그룹 없이 쓰지 않는다(#99). 첫 규칙: SKT(통신3사 일괄 포함) 국정감사 증인·참고인 → 최소 긴급, 09-15~25 실측 11건 적중·오탐 0·AI가 10건은 이미 긴급. (배경역사 #216)
- **배경역사 #216**: 계기(9/22 국감 증인 보도), 실측표(1-2·1-3·1-4), 층 정의, 결정 D1~D7과 이유, 2단계 미결 ①~⑦, 사내판 인계 여부.

---

## 8. 사내판 — 팀별 규칙 편집기를 사내 콘솔에도 둔다 (2026-09-25 운영자 결정)

사내판은 DB 없이 JSONL 파일 위에서 이미 같은 5층 규칙 구조를 돌린다(§1-6). `news_grade.py`는 AI 호출 대상만 고르고(규칙 적중분 제외), 규칙 적용은 콘솔 적재 색인·콘솔 화면 계산·알림 앱 계산 세 곳이며 셋 다 매처 하나로 바꾼다. 사내 콘솔(`rp-console`)의 편집기는 **공통 규칙을 「같게/다르게」로 상속**한다(같게 = 외부 원본 추종, 다르게 = 그 팀만 수정·끔 + 원본 변경 시 「원본이 바뀜」 표시). **원칙은 "규칙 하나에 주인 하나"**:

| 규칙 | 주인(편집하는 곳) | 사내판에서의 취급 |
|---|---|---|
| 공통 규칙(team_id null) | 외부 대시보드 관리자 | 다리(`export_news.py`, anon 키 REST)가 `urgency_rules`의 공통 행을 받아 사내 표에 **읽기 전용**으로 둔다. 사내 `news_grade.py`가 공통 AI 판정 뒤에 적용 |
| 사내 팀 규칙 | 사내 콘솔의 그 팀 편집기(같은 형식의 JSONL, 주인 키 = 조직 코드 문자열, 팀장 = 운영자 명부) | 사내 팀 등급에만 적용. **외부로 올라가지 않는다** — 외부 규칙을 사내에서 고치려면 사내 앱에 쓰기 자격을 두거나 anon 쓰기를 열어야 하는데 둘 다 금지(#169-보론2·9/21 서비스 키 원칙), 흐름은 외부→사내 한 방향 유지 |
| 외부 팀 규칙(2단계 ⑦) | 외부 대시보드 팀장 | 사내판은 받지 않는다(외부 팀과 사내 조직이 1:1이 아님) |

조건 셋(어기면 같은 규칙이 두 판에서 다른 답을 낸다):
1. **형식·매처 동일** — §4-4의 `urgency_rules.py`/`urgency_rules.js`와 `tests/fixtures/urgency_rules_cases.json`을 그대로 이식하고 사내에서도 그 케이스 파일로 검증한다.
2. **공통 규칙은 외부 정본, 사내는 받아 읽기만** — 사내 편집기에서 공통 행은 회색(열람)으로 보이고 "외부 대시보드에서 관리"라고 적는다.
3. **적중 기록·출처 표시도 같이** — 사내 등급에도 `urgency_rule`(공통 규칙 id) / 팀 규칙 id를 남기고 콘솔 배지 옆에 `AI / 규칙: note / 담당자`를 보인다(#180 원칙 "판정은 한 곳에서, 사내는 옮기기만"은 **알림 대표 판정**에 관한 것이고 등급 판정은 이미 사내가 따로 하므로 충돌 없음 — 다만 공통 규칙을 사내가 따로 만들지 않는 것으로 그 취지를 지킨다).

그 밖의 영향:
- 1단계 외부 `news_feed.urgency` 저장값 변화는 스냅샷·다리로 **자동 반영**(사내 작업 없음). 새 컬럼 `urgency_rule`은 다리가 옮기면 출처 표시에 쓰고, 안 옮기면 무해. TOKTOK 알림은 `alert_suppress_log`만 보므로 무관.
- 2단계 외부 팀 값(`team_urgency`)은 사내와 무관. 사내 콘솔이 외부 팀 등급까지 보이려면 그때 스냅샷 TABLES + ported.js 인계.
- **인계문 2장**: ① 사전 질의·예고(`docs/인계_사내판_긴급도규칙_사전질의_260925.md`, 2026-09-25 작성 — 질문 3개와 형식 초안) ② 본 인계는 1단계 구현에서 형식·매처·케이스 파일이 확정되는 시점에 작성(`docs/인계_사내판_긴급도규칙편집기_2609xx.md`, 커밋하지 않는 사내 문서가 아니라 형식·코드 설명만이므로 커밋 가능). 내용: 표 DDL(사내 DB용으로 team FK만 사내 팀 표로), 매처 두 파일, 케이스 파일, 편집기 UI 요구(공통 회색·팀 탭·미리보기), 다리에 공통 규칙 반입 추가, `news_grade.py` 삽입 지점.
- 사내 준비(형식 승인 뒤, 외부 1단계와 병렬): 다리 컬럼 내성 + `urgency_rules` 공통 행 반입(파일 통째 교체, 실패 시 직전 파일 유지) · 매처 이식(케이스 파일 검증) · 세 지점 교체 · 팀장 명부 · 같게/다르게 편집기 · 콘솔·알림 전체 대조 불일치 0. 사내 공통 규칙 줄은 **외부 반입 확인 뒤** 지운다(공백 없게).
- 질의 3개는 2026-09-25 회신으로 해소(§1-6). 외부→사내 회신: `docs/인계_사내판_긴급도규칙_회신_260925.md`.

---

## 9. 구현 순서 (승인 뒤)

1. `crawler.py` 규칙 로더·순수 함수·삽입·기준문 1항목 → 단위 테스트 6+ → 실DB 드라이 대조(11·0).
2. DB: `alter table news_feed add column urgency_rule text` + `urgency_rules` 표(DDL·RLS·GRANT) → `tools_release.py` ⑥ 확인 → 첫 규칙 1행 삽입.
3. (D4) 공용 매처 `urgency_rules.py`·`_shared/urgency_rules.js` + 케이스 파일 → app.js 출처 표시 + 편집기(공통 탭만 활성)·미리보기 → `node --check` → 캐시 버스터(app.js·index.html의 새 스크립트, `.gitlab-ci.yml` cp 목록·`tools_release.py` STATIC 추가).
3-1. 사내판 본 인계문(DDL·매처 두 파일·케이스 파일·`urgency_rule` 기록 규칙·id 불변·enabled=false 삭제 정책) 작성 → 맥북 세션 전달. (D8 ⓐ면) 사내 규칙 5개를 외부 표에 삽입하되 note는 외부 관점 문장으로 다시 쓴다.
4. (D5) 재적용 스크립트 1회(값 변경 1건, 알림 0) → 대시보드에서 출처 표시 확인.
5. 문서 3종 갱신(CLAUDE.md·지침·배경역사 #216) → `tools_release.py` → 커밋·푸시(3단계 검증) → `--verify`.
6. `importance_feedback` 53건 세션 재채점(기준문 변경 확인, API 0회).
7. 다음 크롤 실행 로그에서 `[규칙] 1개 로드(db)`·적중 로그 확인. 10월 국감 출석 보도에서 적중·오탐 재점검 후 2단계 결정.
