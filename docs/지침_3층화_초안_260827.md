# 지침 3층화 초안 (2026-08-27)

> 목적: 지침 194KB(≈6.5만 토큰)의 "매 작업 통독"을 없애고, 세션 시작 비용을 1만 토큰 이하로.
> 근거: Anthropic 공식 권고 CLAUDE.md 200줄 이하 / 명령 150~200개 초과 시 준수율 실측 하락(IFScale) /
> `@`-임포트는 시작 시 전부 로드라 절감 없음 — 진짜 지연 로딩은 경로 스코프 규칙·온디맨드 포인터뿐.
> **원칙: 어떤 규칙도 삭제하지 않는다. 위치만 바꾼다.**

## 1. 목표 구조

| 층 | 파일 | 로드 시점 | 내용 |
|---|---|---|---|
| **1층** | `전파정책AI_지침_핵심.md` (~200줄) | 항상 (CLAUDE.md가 지시) | 시스템 한 장 요약 + 세션 규칙 + 공통 하지말것 ~30개(한 줄씩) + **2층 트리거 표** |
| **2층** | `.claude/rules/*.md` 10개 | 해당 파일을 만질 때 자동 (paths 프론트매터) | 작업별 상세 규칙·절차 (현 지침의 59%) |
| **3층** | `docs/참조/*.md` + 배경역사 | 명시적으로 읽을 때만 | DB 스키마 표·cron 표·체크리스트·외부 키 목록·배경역사(목차 신설) |
| **훅** | `.claude/settings.json` | 도구 호출 시 기계 강제 | 기계적 금칙 ~7개 — 프로즈에서 강등, 안전은 강화 |

- CLAUDE.md 수정: "두 문서를 먼저 읽어라" → "핵심.md만 필수. 상세는 2층이 자동 로드, 참조는 3층 표에서 찾아 읽어라. 배경역사는 #N 번호로만 조회."
- 배경역사: 목차(112항목 번호+제목) 신설 + #88~112 볼드 문단을 `## #N` 헤딩으로 통일.
- 기존 `전파정책AI_지침_운영핵심.md`는 이관 완료 후 `docs/archive/`로 (원본 보존).

## 2. 훅 전환 (기계 강제 — 프로즈는 1층에 한 줄만 남김)

| # | 규칙 | 훅 |
|---|---|---|
| H1 | `git add -A`/`git add .` 금지 | PreToolUse(Bash) 정규식 차단 |
| H2 | bare `python` 금지(3.12 전체 경로) | PreToolUse(Bash) 차단 + 안내 |
| H3 | `.bat` 저장 후 바이트 검증(비ASCII=0·bareLF=0) | PostToolUse(Edit/Write, *.bat) 검사 스크립트 자동 실행 |
| H4 | 크롤러 수동 실행 전 프록시 해제 | PreToolUse(Bash, `python .*crawler`) 경고·차단 |
| H5 | `git push` 후 원격 대조 리마인드 | PostToolUse(Bash, git push) 안내 출력 |
| H6 | `create_client` 직접 호출 금지(sb_client 경유) | PostToolUse(Edit/Write, *.py) grep 검사 |
| H7 | `git commit` 시 커밋 대상에 `.env` 등 시크릿 파일 차단 | PreToolUse(Bash) |

## 3. 하지말것 분류표 (지침 762~966행 전 항목)

분류: **[1]**=1층 유지(한 줄 압축) / **[2-파일명]**=2층 이동 / **[H]**=훅 전환(1층 한 줄+훅) / **[3]**=3층 참조로

### [1] 1층 유지 — 30개 (모든 작업 공통)
1. 일회성 AI 작업은 세션에서, Edge Function 수동 호출 금지(API 과금) (#111)
2. 조용한 실패 금지 — 판정·필터·삭제는 "틀리면 뭐가 보이나"부터 설계 (#108 등 5례)
3. select 목록에 조건 컬럼 누락 금지(조건 무음 무력화)
4. PostgREST: 요청당 1,000행 절단 — 대량 조회는 order+range 페이징, limit에 정렬 생략 금지 (#53·#66·#71·#28 통합)
5. LLM 응답 검증 없이 DB 저장 금지(원문 미보존 테이블은 생성 시점이 유일 방어선) (#99)
6. 폴백은 품질까지 설계(적재 성공률≠품질) (#99)
7. API 키 하드코딩 금지 / `app_config`에 claude_key 재생성 금지(anon 노출) (#104)
8. anon에 새 쓰기 권한은 "화면이 실제 하는 연산"인지 확인 후(공개 Pages=전 세계 공개) (#57)
9. RLS 정책은 `anon, authenticated` 둘 다(로그인 사용자만 0행 사고) + 점검 쿼리는 3층 (#108)
10. RLS 테이블에 프런트 직접 delete 금지(0건 성공으로 무음 실패) (#48)
11. Supabase 클라이언트는 `sb_client.make_client`만 (#15)
12. 텔레그램·임베딩은 공용 유틸(notify/embed_util)만, 수정 시 unittest (#58)
13. 파일 통재작성은 원자적 쓰기(임시→교체) (#51)
14. 대량 배치 띄우기 전 동일 작업 실행 여부 확인 / 중복 정리 DELETE는 범위 명시+행수 확인 (#99)
15. 한국어 키워드 단순 `in` 매칭 금지(앞뒤 글자 확인, 실측 오탐만 차단) (#99)
16. 커밋: 3단계 검증(파일 끝 확인→명시 add→push 후 원격 대조), 동시 커밋 1세션만 (#37)
17. push는 gitlab+origin 둘 다, 배포 시 캐시버스터 갱신
18. `.bat`=ASCII+CRLF, 편집 후 바이트 검증 [H3] (#22)
19. 스케줄러·배치는 python 전체 경로 [H2] (#22)
20. 크롤러 수동 실행 전 HTTP(S)_PROXY 해제 [H4]
21. requirements.txt 고정 유지(무고정 최신화 금지) (#15)
22. Supabase 신규 프로젝트 금지 / Pro 플랜, 병목은 RAM 2GB — 대량 적재 후 REINDEX(디스크 여유 확인 먼저) (#69·#72)
23. Sonnet 5: 비스트리밍은 thinking disabled + `content.find(text)`, `content[0]` 가정 금지
24. 자문·보고서 스트리밍(stream:true) 유지 — 비스트리밍 회귀 금지
25. Sonnet 긴급도 분류 업그레이드 제안 금지(Haiku+피드백으로 충분)
26. news_feed 수동 정리 시 `AND locked=false`
27. 같은 문서 다른 카테고리 중복 업로드 금지 / 법령 구버전 KB 잔류 금지
28. 문서 갱신 규칙: 구조 변경 시 핵심 지침(규칙 한 줄)+배경역사(경위) 동시 갱신
29. 배경역사는 통독 금지 — 목차에서 #N 찾아 해당 항목만
30. 기준문 수정 시 자매 기준문 3종(news/assembly/press) 같은 구멍 확인 (#102·#107)

### [2-crawlers] → `.claude/rules/crawlers.md` (paths: crawler.py, gov_notice_crawler.py, press_ingest.py, foreign_press.py, refetch_content.py, news_dedup.py, issue_suggest.py)
- 뉴스 클러스터링: 별형(cluster_star)만·연쇄 금지·임계 3 유지·event 라벨 금지
- 재알림 억제·클러스터링 fail-open 유지 / 긴급 fan-out은 억제·클러스터링 뒤에
- 해외(category='해외') 60일 삭제 예외 2지점 + select 컬럼 동반
- crawl_naver_news=OpenAPI(스크래핑 회귀 금지) / crawl_msit=인라인 스크립트 정규식+bbsSeqNo / MSIT 첨부 3-인자+POST+Referer
- KCC_KEYWORDS 유지 / kmcc.go.kr=방미통위(전파관리소는 crms.go.kr) / jsessionid 제거 / 게시판 순환 방어(fingerprint) / fcc rss 가짜·Ofcom 직접 수집 금지
- 스포츠 오탐은 is_sports_noise 패턴(키워드 축적 금지, 수정 시 DB 대조)
- 기준문은 app_config이 원본(코드 FALLBACK은 비상용) / 긴급도 제1원칙(주체·파급)·순서 의미·포괄 규칙에 제외 명시 / 피드백 학습에 기준 미루기 금지 / 낱말 동형 이분야 함정(혼신→전파간섭, 중계기)
- upsert(on_conflict='url') 유지 / fetch_article_body 1500자 절단(지식 수집엔 trafilatura 직접) / 뉴스 무관 판정 "수단·배경만이면 무관" 규칙 양쪽 유지
- gov_notice_crawler는 Actions 이전 금지(한국 IP) / 이슈 제안·연결 규칙(휴면=links.created_at, rejected 재제안 금지 등 — 현 이슈맵 섹션 병합)

### [2-assembly] → `rules/assembly.md` (paths: assembly_*.py, law_diff_gen.py, summarize_assembly_bills.py)
- 입법예고=OpenAPI 전량 스캔(+소관부처 보강), lsNm 검색·pal 스크래핑 회귀 금지 / "○○부 소관" 접두 제거 유지
- backfill_opinion_summaries·브리핑 summary 유지 / 회의록은 뷰어(xml.do) 정본·PDF 폴백 / billId URL 폴백 유지 / 국감 confer_num 네임스페이스

### [2-law-kb] → `rules/law-kb.md` (paths: law_*.py, add_law*.py, upload_law_pdf.py, import_regulatory_kb.py, kb_reextract.py, sync_*.py, backfill_embeddings.py)
- law_crawler 엔드포인트 DRF 고정 / '혼신' 재추가 금지
- 임베딩 모델 매핑(kb=voyage-law-2, document_chunks=voyage-4-lite) 혼용 금지 / superseded 청크 임베딩 제거 유지 / only_current 기본
- kb_*↔document_chunks 상호보완(합치기 금지) / manifest 정본 / 재적재 전 sync_kb_to_bundle / OKF 자동화 순서(--all-outdated 전 OKF 먼저)
- 법령은 API로(대시보드 PDF 업로드 금지, ITU-R만 업로드) / ITU-R 자동 다운로드 금지(저작권) / 영문 원문은 한국어 요약 짝
- 별표: buildAnnexContext 상한 3종 / 타 법령 별표 인용 추적 금지 / 머리말 재적재 해법 기각
- PDF 흔적 정리 3곳 동일 유지 / OCR 저해상도 등재 반려
- 관계도 빌더: 노드 일괄 삭제 금지(엣지만 재구축) / 병합 원칙(삭제 없음) / 고시 접기+고아 주제 구제 / lawmapNeighborhood 확장 금지 / law_topic·주제 큐레이션 규칙(이슈맵 섹션에서 이동)

### [2-frontend] → `rules/frontend.md` (paths: app.js, index.html, styles.css, system_prompt.js)
- 캐시버스터(app.js?v= / system_prompt.js?v=) / 사이드바 overflow-y / '보고서 초안' 숨김(삭제 아님)
- loadNews 전량 페이징+잠금 병합 / _KB_GROUPS 문자열 매칭 확인 / 목록 검색은 캐시 매칭(타자마다 DB 금지) / guide body_md 통재수신 금지 / 조문 검색줄 복원 / 계층 목록 유지
- renderMd 표 처리(빈 열·통칸)+컨테이너 스타일(.md-body) / 하드코딩 문서 설명 사실 검증
- 자문: 검색 병렬 유지 / 뉴스 발췌 차등(1800/700)·스코어링·수치 인용 지시 / 국회 동향 위치 / 키워드 함수 2종 분리 / 조문 검색 .pdf·.md 제외 / lawmap 블록 지침 유지 / kb 출처 배지 기록 / 시제 가드·현지어 검색(수정 시 sync_system_prompt+버스터)

### [2-edge] → `rules/edge-functions.md` (paths: supabase/functions/**)
- CORS 3함수 동시 수정 / operator-webhook·telegram-webhook은 no-verify-jwt+secret 검증이 관문 / webhook은 항상 200 / env .trim()
- claude-proxy: getUser가 관문(verify_jwt 아님) / stream 판별은 body / TransformStream+waitUntil / 쿼터 원자적 증가·service_role 전용 RPC
- 봇: 토큰 2종 구분 / fail-closed 게이트(`!sub?.권한`) / 신규 제약 소급 허용 / 피드백 버튼 마지막 조각·fb:rating:id / chat_logs channel / 구독자봇 이슈맵 노출 금지

### [2-scheduler] → `rules/windows-scheduler.md` (paths: *.bat, run_hidden.py)
- pythonw run_hidden 경유(콘솔 창 금지) / 배터리 3종+StartWhenAvailable / 작업 경로 radio-policy-ai / --user pip 금지(-s 검증) / stdout UTF-8 강제 / Cowork 재등록 금지
- temp_gh_law.bat promote·watch 2줄(계정 복구 시 삭제) → 복구 체크리스트와 함께 3층으로도 링크

### [2-briefing] → `rules/briefing.md` (paths: morning_briefing.py, send_briefing.py, resend_briefing.py, subscriber_notify.py, health_watchdog.py)
- 빈 브리핑 폴백·무뉴스 통지·placeholder 교체 유지 / cron 21UTC / KST 배지 / parseBriefingContent 처리 / 브리핑 Haiku 긴급 재위임 금지 / 영향도가 긴급도 덮어쓰기 금지
- 워치독: watchdog_scan 분리 유지·jobid16·state 키 / new=0 오탐 금지 / DB 신선도 단독 회귀 금지 / heartbeat 3종

### [2-accounts] → `rules/accounts-rls.md` (paths: supabase/**, app.js — 계정·RPC 작업 시)
- 관리자 비번 체계 부활 금지(is_admin 단일 관문) / Confirm email 금지 / admin 공백 금지 / 가입 알림 트리거(예외 삼킴·timeout 10s)
- chat_logs anon SELECT 금지·RETURNING 불가(id는 클라이언트 생성) / answer_feedback RPC 전용 / report_feedback anon INSERT 금지 / 👍👎 비의무
- 학습 데이터 4종(report_*)·deleted_news 등 비우기 금지

### [3] 3층 참조로 이동
- DB 스키마 표(140줄) → `docs/참조/DB스키마.md` (+"실DB 확인이 우선" 머리말)
- pg_cron 잡 표·알림 채널·외부 서비스·키 → `docs/참조/운영표.md`
- 점검 체크리스트·알려진 제약 12종 → `docs/참조/점검체크리스트.md`
- GitHub 계정 복구 체크리스트(⑦단계)·백업 소유자 → `docs/참조/계정복구.md`
- 배경역사: 목차 신설, 헤딩 통일 — 항상 로드 대상에서 완전 제외

## 4. 적용 순서(안)

1. 훅 7종 작성·테스트 (기존 지침 그대로인 상태에서 — 안전망 먼저)
2. 2층 rules 파일 10개 생성(내용은 현 지침에서 이동·압축) + 3층 참조 파일 4개 분리
3. 1층 핵심 지침 작성(~200줄) + CLAUDE.md의 "먼저 읽기" 문구 교체
4. 배경역사 목차·헤딩 정리
5. 1주 병행 관찰(구 지침은 archive에 보존) 후 이상 없으면 완료 — 문제 시 즉시 원복 가능

## 5. 남는 결정 (운영자)

- **훅의 강제 수준**: 차단(deny) vs 경고(warn) — 권장: H1·H2·H7 차단, H3~H6 경고+안내
- **1층 30개 목록 승인**: 위 [1] 목록에서 빼거나 2층에서 올릴 항목
- **적용 시점**: 한 번에 vs 훅부터 단계적(권장)
