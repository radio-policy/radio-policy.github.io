-- ============================================================================
--  docs/schema.sql — 폐지 (2026-09-26, 개선안 §4-4-11, 배경역사 #227)
-- ----------------------------------------------------------------------------
--  이 파일은 옛 '강의용 설치 스크립트'였다. 실DB와 크게 어긋나 있었고(표 27/49·함수 14/63·
--  정책 3/79·예약 작업 0/16, 지운 report_* 표 포함), 옛 '대시보드는 anon' 권한 모델(anon 전체
--  쓰기 정책·anon 쓰기 GRANT)이라 새 프로젝트에 돌리면 #226에서 막은 공개 키 쓰기가 다시 열렸다.
--
--  DB 설계도의 정본은 docs/db_baseline/ 이다.
--    - py -3.12 tools_db_baseline.py 가 실DB 카탈로그에서 만든다(비밀 마스킹, 데이터 없음)
--    - 복구 순서는 docs/db_baseline/README.md
--    - DB를 바꾼 세션은 커밋 전 py -3.12 tools_release.py ⑦ 로 설계도가 최신인지 본다
--
--  이 파일에는 실행할 문장이 없다(주석뿐). 옛 내용은 git 이력에 있다.
-- ============================================================================
